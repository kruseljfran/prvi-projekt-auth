<!DOCTYPE html>
<html lang="hr">
  <head>
    <meta charset="UTF-8" />
    <title>Loto Aplikacija</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <h1>Loto 6/45</h1>

    <% if (user) { %>
      <p>Prijavljeni ste kao: <strong><%= user.name %></strong></p>
      <a href="/logout">Odjava</a>
    <% } else { %>
      <a href="/login">Prijava</a>
    <% } %>

    <hr />

    <div class="box">
      <% if (round) { %>
        <p><strong>ID kola:</strong> <%= round.id %></p>
        <p><strong>Status:</strong> <%= round.active ? "Uplate aktivne" : "Uplate zatvorene" %></p>

        <% if (round.draw_numbers) { %>
          <p><strong>Izvučeni brojevi:</strong> <%= round.draw_numbers.join(", ") %></p>
        <% } else { %>
          <p>Još nema izvučenih brojeva.</p>
        <% } %>
      <% } else { %>
        <p>Još nema evidentiranih kola.</p>
      <% } %>
    </div>

    <% if (user && round && round.active) { %>
      <hr />
      <a href="/ticket">Uplati listić</a>
    <% } %>
  </body>
</html>
